import { Module } from '@nestjs/common';

// BUS-*: business onboarding, principal capture, affiliation confirmation, sole-trader
// inline journey. Schema exists (migration 0003); repository and HTTP layer are Epic 4.
@Module({})
export class BusinessesModule {}
